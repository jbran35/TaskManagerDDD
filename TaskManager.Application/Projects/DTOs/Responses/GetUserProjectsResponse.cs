namespace TaskManager.Application.Projects.DTOs.Responses
{
    //A DTO for returning a user's projects as a list of the projects' tile views
    public record GetUserProjectsResponse
    {
        //public List<ProjectTileDto> UserProjectTiles { get; init; } = new List<ProjectTileDto>();

    }
}
//